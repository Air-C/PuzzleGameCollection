using PGC.ModuleInventory.SO;
using Unity.VisualScripting;
using UnityEditor;
using UnityEngine;
using UnityEngine.AddressableAssets;

namespace Editor
{
    public class GMCmd
    {

        // [MenuItem("GMCmd/读取表格")]
        // public static async void ReadTable()
        // {
        //     var handle = Addressables.LoadAssetAsync<itemTable>("itemTable");
        //     await handle.Task;
        //     itemTable table = handle.Result;
        //     foreach (var item in table.itemList)
        //     {
        //         Debug.Log($"id : {item.Value.ID}, name : {item.Value.Name}, description : {item.Value.Description}, abilityType : {item.Value.AbilityType}");
        //     }
        // }
    }
}